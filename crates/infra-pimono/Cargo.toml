[package]
name = "infra-pimono"
version = "0.1.0"
edition = "2024"

[dependencies]
app-core = { path = "../app-core" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
shared-kernel = { path = "../shared-kernel" }
